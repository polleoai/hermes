{
  "name": "hermes-obsidian",
  "version": "1.0.0",
  "description": "Claude chat for Obsidian with vault-aware tools. Uses the Anthropic API; optional local Claude CLI for advanced users.",
  "main": "main.js",
  "private": true,
  "license": "MIT",
  "author": "POLLEO.AI",
  "repository": {
    "type": "git",
    "url": "https://github.com/polleoai/hermes"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "claude",
    "anthropic",
    "llm",
    "ai",
    "chat"
  ],
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --test tests/*.test.js",
    "build:install": "node build.js && echo 'Built. Copy main.js + manifest.json + styles.css into your vault .obsidian/plugins/hermes/ to install.'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "undici": "^8.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
